/**
 * Общие сведения о сайте: используются в метаданных, разметке и футере.
 * Единственный источник правды — менять здесь, а не по месту.
 */
export const siteConfig = {
  name: "РусКабель",
  description:
    "Каталог кабельно-проводниковой продукции: характеристики, цены и подбор кабеля по параметрам.",
  url: process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000",
} as const;
